SELECT DISTINCT * WHERE{
	GRAPH <{{flod.dataGraph}}>{
		{
			?s <{{flod.labelPredicate}}> ?sName .
			<{{"/".join(args)}}> ?p ?s .
			}UNION{
			?s <{{flod.labelPredicate}}> ?sName .
			?s ?p  <{{"/".join(args)}}> .				
			}
	}
}
